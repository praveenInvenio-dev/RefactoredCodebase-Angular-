import { SuffixdatePipe } from './suffixdate.pipe';

describe('SuffixdatePipe', () => {
  it('create an instance', () => {
    const pipe = new SuffixdatePipe();
    expect(pipe).toBeTruthy();
  });
});
